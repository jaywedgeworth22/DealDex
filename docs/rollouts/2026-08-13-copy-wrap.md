# 2026-08-13 — Copy wrap and guest line

Seat: GROK-BUILD

Settings intro was a `max-w-xl` rag with the guest clause spliced mid-sentence.
Scan-card blurb sat beside the marketplace toggles, so it wrapped at “in /
Settings.” Hero used a second, narrower max-width.

- Settings, hero, and scan blurb use a full `max-w-3xl` measure
- Marketplace toggles sit on the title row, not beside the paragraph
- Guest line is `N extra desks enabled — GUEST` (or `SIGNED IN`)
- Page title is API Desks
