# 2026-09-01 — Datadog env canonicalize (Grok, `grok/datadog-free-tier`)

`DD_ENV=prod` maps to `production` so DealDex rows join the rest of the fleet dashboards.  RUM stays dark without tokens.  No new SKUs.
