# 2026-08-13 — One page measure

Seat: GROK-BUILD

Intro copy was wrapping inside `text-pretty` / `max-w-3xl` while the scan
card used the full shell. Looked like a skinny rag above a wide card.

- Shell is `max-w-7xl` (1280px) — about two-thirds of a 1920 Mac display
- Page leads and card blurbs use that same width
- `text-pretty` removed (it shortens lines on purpose)
- Marketplace toggles sit next to the scan title, not flung to the far right
