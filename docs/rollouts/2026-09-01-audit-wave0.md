# 2026-09-01 — DealDex audit Wave 0

Pull the pre-fix APK and source zip off production.  Add robots/sitemap.  Stop SVG logos from injecting extra `<title>` tags.

## Why

Live `GET /DealDex.apk` was 200, 17.8 MB, CORS `*`.  `/install` warned the file was unsafe and still offered Download.  Crawlers 404'd robots as HTML.

## Verify after deploy

```
curl -sI https://dealdex.net/DealDex.apk
curl -sI https://dealdex.net/robots.txt
curl -sL https://dealdex.net | grep -c '<title>'
```

Expect APK 404, robots 200 text/plain, one document title.
