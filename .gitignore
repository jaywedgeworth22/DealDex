node_modules/
dist/
.output/
.vinxi/
.tanstack/
.vercel/
.data/
.android-sdk/
.grok/
.project_id
.node_modules.lock
*.log
/tmp/
screenshots/
attachments/
artifacts/
native/android/.gradle/
native/android/app/build/
native/android/build/
native/android/local.properties
native/ios/**/xcuserdata/
native/ios/**/*.xcuserstate
public/spreaddex-android-debug.apk
public/spreaddex-native-src.zip
public/dealdex-android-debug.apk
public/dealdex-native-src.zip
.env
.env.*
!.env.example
.DS_Store
*.icloud

# Apple private keys (App Store Connect API keys, APNs auth keys). These live in ~/.secrets/
# ONLY. A .p8 dropped in the repo root is untracked but NOT ignored without this rule, so a
# single `git add -A` would stage a credential that signs pushes for every app in the team.
# One APNs key is team-wide, so leaking it is not scoped to one app.
*.p8

AGENTS.project.md
